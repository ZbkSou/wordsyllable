-- ================================================
-- 数据库迁移脚本 - 添加自然拼读解析和词根词缀字段
-- ================================================
-- 创建日期: 2025-11-19
-- 说明: 为 words 表添加两个新字段
--       1. phonetic_analysis - 自然拼读解析
--       2. root_affix - 词根词缀
-- ================================================

USE word_memory;  -- 请根据实际数据库名称修改

-- 添加自然拼读解析字段
ALTER TABLE words 
ADD COLUMN phonetic_analysis TEXT NULL COMMENT '自然拼读解析' 
AFTER phonetic;

-- 添加词根词缀字段
ALTER TABLE words 
ADD COLUMN root_affix TEXT NULL COMMENT '词根词缀' 
AFTER phonetic_analysis;

-- 查看修改后的表结构
DESC words;

-- ================================================
-- 注意事项：
-- 1. 执行前请先备份数据库
-- 2. 这两个字段允许为空(NULL)，不会影响现有数据
-- 3. 新添加的单词将包含这两个字段的值
-- ================================================

